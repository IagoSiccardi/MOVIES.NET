<!DOCTYPE html>
<html lang="es">
    <%- include("./partials/head.ejs",{
        title: "MOVIES.NET | NEWEST"
      }) %> 
<body>
    <%- include("./partials/header.ejs") %> 

     <main class="movies_main">

        <h2 class="movies_h2">Estrenos</h2>

        <section class="movies_cards">
            <% for( let i = 0; i < movies.length; i++ ) { %>
                
                <article class="card">
                    <a href="/movies/detail/<%= movies[i].id %>">
                        <img src="/img/movies/<%= movies[i].image %> " alt="">
                    </a>
                    <span><%= movies[i].title %> </span>
                </article>
            <% } %>

            
            
            
        </section>
        <h3 class="genres_h3"><a href=""> Ver todas los peliculas</a></h3>
        
    </main>

    <%- include("./partials/footer.ejs") %> 

</body>
</html>